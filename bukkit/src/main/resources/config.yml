# 检测到断开后等待多少秒再重连
reconnect_delay: 15

# 定期健康检查间隔（秒），0=禁用。主动执行重连命令检测连接状态
health_check_interval: 300

# 最大重连次数，0表示无限重连
max_attempts: 0

# 当无法解析封禁解封时间时的默认等待时间（秒）
ban_wait_time: 600

# 是否启用自动重连
enabled: true